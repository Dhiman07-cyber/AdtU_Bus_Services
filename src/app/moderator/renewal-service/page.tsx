"use client";

import AdminRenewalServicePage from '@/app/admin/renewal-service/page';

// Moderators have the same renewal management interface as admins
// The access control is handled within the component based on user role
export default AdminRenewalServicePage;