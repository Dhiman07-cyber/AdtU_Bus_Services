"use client";

// Student renewal service page with 2 tabs
// Tab A: Renewal Service (select duration, pay online/offline)
// Tab B: Transaction History (from JSON files)

import StudentRenewalPage from '@/app/student/renew/page';

export default StudentRenewalPage;
