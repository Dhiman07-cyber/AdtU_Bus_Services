# =============================================================================
# ADTU Smart Bus Management System - Test Environment Variables
# =============================================================================
# Copy this file to .env.test and fill in the values for your staging environment.
# NEVER commit .env.test with real values to version control.
# =============================================================================

# ===== Target Environment =====
STAGING_URL=https://your-staging.vercel.app
PRODUCTION_URL=https://adtu-bus.vercel.app

# ===== Authentication Tokens =====
# Generate these tokens by logging in as each user type and extracting JWT from session
# These should be short-lived tokens from your STAGING environment only

# Admin token (full access)
TEST_ADMIN_TOKEN=your_admin_jwt_token_here

# Moderator token (can manage students/drivers but not admins)
TEST_MODERATOR_TOKEN=your_moderator_jwt_token_here

# Driver token (can broadcast location, view routes)
TEST_DRIVER_TOKEN=your_driver_jwt_token_here

# Student token (can view buses, submit applications)
TEST_STUDENT_TOKEN=your_student_jwt_token_here

# Test student UID (for idempotency and audit tests)
TEST_STUDENT_UID=test_student_001

# ===== Payment Testing =====
# Razorpay webhook secret (staging/test mode secret)
RAZORPAY_WEBHOOK_SECRET=your_razorpay_webhook_secret

# Test payment ID for replay tests
TEST_PAYMENT_ID=pay_test_123

# ===== Supabase (for k6 direct tests) =====
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key

# ===== Feature Flags =====
ENABLE_FIRESTORE_REALTIME=true

# ===== Vercel Deployment =====
VERCEL_TOKEN=your_vercel_token
VERCEL_ORG_ID=your_org_id
VERCEL_PROJECT_ID=your_project_id
